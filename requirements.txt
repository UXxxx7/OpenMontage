# OpenMontage - Core Dependencies
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.20
python-dotenv>=1.0
Pillow>=10.0
numpy>=1.24
requests>=2.31
google-auth>=2.0       # service-account auth for Google TTS + Imagen (Vertex AI)
qrcode>=8.0            # QR code generation for the WhatsApp contact/CTA close (whatsapp_mvp/qr_gen.py)
opencv-python>=4.9,<5  # Haar-cascade face detection fallback for tools/analysis/face_tracker.py (speaker crop calibration) — v5 dropped cv2.CascadeClassifier, don't widen past <5 without updating that tool
